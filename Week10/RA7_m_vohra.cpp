#include <iostream>
#include <fstream>
#include <queue>
#include <stack>
#include <string>

using namespace std;

bool inLanguage(string & inputString) {

    if(inputString.empty()) {
        cout << "string is missing #." << endl;
        cout << "\"" << inputString << "\" " << "is not a sentence in the Language." << endl;

    }
}